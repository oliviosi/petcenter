name: tenant-domain-health-dashboard
summary: Criar um dashboard de saúde de domínio por tenant com falhas de notificação, prontidão de certificado e verificação de DNS.
apply:
  requires: ["proposal", "tasks", "runbook"]
artifacts:
  - id: proposal
    type: markdown
  - id: tasks
    type: markdown
  - id: runbook
    type: markdown
  - id: pr_description
    type: markdown
  - id: ops_apply_instructions
    type: markdown
